/** Shared with auth forms (login, register, forgot password, etc.) */
export const AUTH_EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const PASSWORD_MIN_LEN = 8;
export const PASSWORD_MAX_LEN = 128;
