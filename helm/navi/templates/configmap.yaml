apiVersion: v1
kind: ConfigMap
metadata:
  name: navi-config
  namespace: {{ include "navi.namespace" . }}
data:
  KAFKA_BROKERS: {{ include "navi.kafkaBroker" . | quote }}
{{- range $k, $v := .Values.config }}
{{- if ne $k "KAFKA_BROKERS" }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
{{- end }}
