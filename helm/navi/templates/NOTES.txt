Navi has been deployed.

Release: {{ .Release.Name }}
Namespace: {{ include "navi.namespace" . }}

{{- if .Values.ingress.enabled }}

Ingress hosts:
  API:   https://{{ .Values.ingress.apiHost }}
  User:  https://{{ .Values.ingress.userAppHost }}
  Admin: https://{{ .Values.ingress.adminAppHost }}
{{- end }}

Images use registry {{ .Values.registry.dockerhubUsername }} with tag {{ .Values.registry.tag }}.

Check status:
  kubectl get pods -n {{ include "navi.namespace" . }}

Database init runs as a Helm post-upgrade hook (Job navi-db-init).
